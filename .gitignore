.next
node_modules
out
dist
.env
.env*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Dump de schema gerado localmente pelo script de homologacao (nao versiona,
# muda a cada execucao e nao precisa acompanhar o historico do repo).
scripts/homologacao/schema-producao*.sql

# Local analysis and generated artifacts that can contain customer data.
.codex_tmp/
outputs/
reports/
tmp/
render_*/
*.m4a

# Office files are ignored at the repository root. Versioned application
# templates and fixtures inside dedicated source folders remain trackable.
/*.xlsx
/*.xls
/*.docx
/*.pptx
